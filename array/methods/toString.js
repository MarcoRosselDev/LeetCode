/* retorna un string representando el 
array especificado y sus elementos 

sintax:

array.toString();
*/
const array1 = [1, 2, 'a', '1a'];

console.log(array1.toString());
// Expected output: "1,2,a,1a"
