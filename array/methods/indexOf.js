/* retorna el primer indice en el que se puede
encontrar un elemento dad en el array,
retorna -1 si el elemento no se encuentra */

// sintax
// indexOf(searchElement, fromIndex)

const array = [3, 3];

const pregunta = array.indexOf(3, 1);
console.log(pregunta);